{
  "name": "project",
  "version": "1.0.0",
  "description": "+ user req to a domain\r   + client side code get the informations (ip, reqested domain)\r   + if server not on maintenance it will do further process\r   + client information encode then sends to server\r   + server decode\r   + after decode server check block ip\r   + server searches to the database for ip of requested domain\r   + if ip not find it sends the not found response\r   + if found ip it encodes data and send response to client\r   + client decode that data\r   + finally send to user with response\r   + after certain time it will redirect to that ip/destination\r   + if user blocked then user cannot request query\r   + after certain time automatically user unblocked\r   + then user can request again",
  "main": "index.js",
  "scripts": {
    "start": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.1.7"
  },
  "dependencies": {
    "dns-packet": "^5.6.1",
    "express": "^4.21.1"
  }
}
